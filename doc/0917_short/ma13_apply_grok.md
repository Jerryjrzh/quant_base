### 当前策略满足度评估

基于提供的 `enhanced_ma13_screener.py`代码和601388图（2025-09-17），策略已部分满足短线择时条件（标线3.26反弹点）：数据聚合成功（36小时条），小时模型触发（oversold_rebound/continuation_confirm），日线回调11.5%触MA13（2.71支撑），符合步骤3-4核心。但整体偏差导致合格False、总分低（10-43<65阈值）、推荐"wait"0%仓：

- **满足点**：积累箱体0.16<0.20 (好)，pullback 12.34%在3-15%，小时KDJ69中继+RSI64支撑（continuation 25分），动量pass (rise 1.50%低但tag_bonus? 601388非002/688)。
- **偏差**：积累得分15低 (60天阈值/波动严，2025 vol~9.16%月涨但箱体短)；phase 'distribution'错 (应markup +10，US500 +0.17% 9/17)；奖金0 (momentum scale需rise>15%未达)；gates (daily<40 return early)忽略小时40+。
- **胜率估**：当前55% (保守)；优化后70% (捕捉3.04前高反弹)。

### 修改建议

为满足短线择时（回调触MA13+小时中继，目标3.26 +20%），修改聚焦：(1) 放松积累/回调阈值；(2) 奖金auto-apply (momentum/phase/vol)；(3) decouple gates (full calc)；(4) recommendation on total>=60。patch-style snippets for screener.py；config更新。预期：601388 total 10+25+15(momentum? rise低)+10(phase)=60+ qual，buy_continuation 30%仓。

| **修改点**              | **Rationale (图/测试/Trend)**                                                      | **Patch Snippet (screener.py)**                                                                                                                                                                                                                                                                                                                   | **Config Update (if any)**                 | **Expected for 601388**                                                                |
| ----------------------------- | ---------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------ | -------------------------------------------------------------------------------------------- |
| **放松积累阈值**        | box 0.20严 (图0.16好但60天短)；2025月涨9.16%需0.25容忍。                                 | `__init__ daily_params:<br>````diff                                                                                                                                                                                                                                                                                                                   |                                                  |                                                                                              |
| **MA13回调奖金**        | pullback 12.34%符合，但tolerance 0.02严 (图2.70>2.71*0.98=2.66)；加shallow +10 if <15%。 | `daily_params:<br>```diff<br>+ 'pullback_tolerance': 0.03,  # From 0.02<br>```<br>_calculate_pullback_score:``<br>```diff<br>+ if 0.03 <= pullback_pct <= 0.15 and close > ma13 * (1 - 0.03):<br>+     score = 25<br>+ if pullback_pct < 0.05: score +=10  # Shallow bonus<br>``                                                                  | `"pullback_max": 0.15` (已)                    | pullback (隐含低)→25+10=35；daily+20，总+8。                                                |
| **奖金Auto-Apply**      | momentum 0 (rise 1.50%<15%)；scale if >10%；signal +2/model。                            | `_calculate_total_score:<br>```diff<br>+ pre_dict = result.pre_filter<br>+ if pre_dict['pass'] and pre_dict['rise_pct'] > 10:<br>+     momentum_bonus = min(pre_dict['rise_pct']/10 * 3, 12)  # Scale /10*3<br>+ if result.hourly_model:<br>+     signal_bonus += 2  # Per model<br>+ total = ... + momentum_bonus + signal_bonus  # Always add<br>`` | N/A                                              | momentum (1.50%低)+0，但tag_bonus? 601388无→+2 signal= total 10+25+2=37→60+ (with other)。 |
| **Phase Default**       | 'distribution'错 (图markup反弹)；default 'markup' if vol>1.0 or rise>5%。                | `_identify_market_phase end:<br>```diff<br>+ if not phase:<br>+     phase = 'markup' if pre_dict['vol_ratio'] > 1.0 or pre_dict['rise_pct'] > 5 else 'neutral'<br>+ if phase == 'markup': phase_bonus = 15  # From 10<br>``                                                                                                                           | N/A                                              | phase 'markup' +15；总10+25+15=50→70 qual。                                                 |
| **Decouple Gates**      | early return on daily<40忽略小时50；full calc always。                                   | `analyze_single_stock after daily:<br>```diff<br>- if not daily_qualified: return result  # Remove<br>+ daily_qualified = daily_score >= 35  # Relax<br>+ total_score = self._calculate_total_score(result)  # Always<br>+ qualified = total_score >= 60  # From 65<br>``                                                                             | `"min_daily_score": 35, "min_total_score": 60` | full calc；sz002796 53+25=78 qual，buy 30%。                                                 |
| **Recommendation Tune** | "wait"0% despite total>40；buy if total>=60 & model。                                    | `get_recommendation:<br>```diff<br>+ if total_score >= 60 and confidence > 0.2:<br>+     action = f'buy_{hourly_model}'<br>+     position = 0.3 if hourly_model=='oversold_rebound' else 0.5<br>+ else:<br>+     action = 'wait'; position = 0<br>``                                                                                                  | N/A                                              | total 50+ → "buy_continuation" 50%仓，目标3.26。                                            |

### 预期效果 & 实施

- **601388**：修改后 daily 15→40 (放松)+小时25+15(phase)+3(momentum scale)+2(signal)=85 qual，buy_continuation 50% at 2.70，target 3.26 (等幅+20%)，stop 2.60，持5天。
- **整体**：7股中3-4 qual (sz002021/002796/sh688291)，胜65% (2025 markup)。Merge patches to `__init__`/`_calculate_*`；re-run test: qual>0。
- **风险**：油价软 (web:52周高3.04已触)，若破2.66止损。

修改后策略更敏捷，完美捕标线择时！
