# 股票画像生成指南

## 概述

股票画像生成系统为每只股票找到最优的技术指标参数，通过优化算法自动调整KDJ、RSI、MACD等指标参数，使其更好地适应个股特性，从而提高交易信号的准确性。

## 核心改进

根据 `doc/test_fix_1.md` 的要求，我们对系统进行了以下优化：

### 1. 修复了参数错误
- 统一了RSI指标的参数名称（从 `periods` 改为 `n`）
- 修复了KDJ指标返回值的处理
- 优化了多进程工作函数的实现

### 2. 增强了多进程处理
- 重构了多进程工作函数，提高了稳定性
- 增加了更好的错误处理和进度监控
- 支持断点续传功能

### 3. 改进了通用筛选器
- 更健壮的参数获取和解析逻辑
- 统一的参数传递机制
- 更好的错误处理

## 脚本说明

### 1. `quick_generate_profiles.py` - 快速开始脚本

**用途**: 最简单的方式为核心观察池生成画像

**特点**:
- 自动检测已有画像，避免重复计算
- 使用多进程加速
- 提供清晰的进度显示
- 适合首次使用或日常维护

**使用方法**:
```bash
python quick_generate_profiles.py
```

**输出示例**:
```
🚀 快速生成股票画像
==================================================
📊 核心观察池股票数量: 50
📈 已有画像股票: 10
🎯 待生成画像股票: 40

🔄 开始生成画像...
💡 使用多进程模式以提高速度
...
✅ 画像生成完成！
⏱️  总耗时: 120.5 秒
📊 成功: 38 只
❌ 失败: 2 只
📈 成功率: 95.0%
🎯 平均验证分数: 0.652
```

### 2. `generate_all_stock_profiles.py` - 完整管理脚本

**用途**: 全功能的画像生成管理工具

**特点**:
- 支持核心池和全部股票池
- 分批处理，支持大规模数据
- 断点续传功能
- 详细的日志记录
- 数据导出功能

**使用方法**:
```bash
python generate_all_stock_profiles.py
```

**功能菜单**:
1. 为核心观察池生成画像 (推荐先执行)
2. 为所有股票生成画像 (耗时较长)
3. 查看画像生成摘要
4. 导出画像数据到JSON
5. 退出

### 3. `test_profile_generation.py` - 测试验证脚本

**用途**: 测试和验证画像生成功能

**特点**:
- 单只股票测试
- 小批量测试
- 数据完整性验证
- 优化算法性能测试

**使用方法**:
```bash
python test_profile_generation.py
```

## 使用流程

### 第一次使用

1. **快速开始** (推荐)
   ```bash
   python quick_generate_profiles.py
   ```

2. **或者使用完整管理工具**
   ```bash
   python generate_all_stock_profiles.py
   # 选择选项 1: 为核心观察池生成画像
   ```

### 日常维护

1. **检查画像状态**
   ```bash
   python generate_all_stock_profiles.py
   # 选择选项 3: 查看画像生成摘要
   ```

2. **更新画像** (如有新股票加入核心池)
   ```bash
   python quick_generate_profiles.py
   ```

### 大规模处理

1. **为所有股票生成画像**
   ```bash
   python generate_all_stock_profiles.py
   # 选择选项 2: 为所有股票生成画像
   # 建议批处理大小: 50-100
   # 建议使用多进程: 是
   ```

## 技术参数

### 优化参数范围
- **KDJ周期**: 5-20 (默认: 9)
- **RSI周期**: 5-30 (默认: 14)
- **MACD快线**: 5-20 (默认: 12)
- **MACD慢线**: 20-50 (默认: 26)
- **短期均线**: 5-20 (默认: 10)
- **长期均线**: 20-60 (默认: 30)

### 优化算法
- **主要算法**: 差分进化 (Differential Evolution)
- **备用算法**: L-BFGS-B
- **目标函数**: 双目标优化 (买入信号与低点距离 + 卖出信号与高点距离)
- **验证方法**: 模拟交易回测

### 性能参数
- **数据要求**: 至少250个交易日
- **优化窗口**: 最近250个交易日
- **验证窗口**: 最近250个交易日
- **多进程**: 支持，推荐使用
- **批处理**: 支持，推荐50-100只股票/批

## 画像数据结构

生成的画像数据包含以下字段：

```json
{
  "kdj_n": 12,
  "rsi_period": 18,
  "macd_fast": 10,
  "macd_slow": 28,
  "ma_short": 8,
  "ma_long": 35,
  "optimization_error": 15.23,
  "optimization_success": true,
  "validation_score": 0.678
}
```

### 字段说明
- **技术指标参数**: 优化后的最佳参数值
- **optimization_error**: 优化目标函数值 (越小越好)
- **optimization_success**: 优化是否成功
- **validation_score**: 验证分数 (0-1，越高越好)

## 性能优化建议

### 1. 硬件配置
- **CPU**: 多核处理器，推荐8核以上
- **内存**: 至少8GB，推荐16GB以上
- **存储**: SSD硬盘，提高数据读取速度

### 2. 运行环境
- **Python版本**: 3.8+
- **依赖库**: 确保scipy、numpy、pandas版本较新
- **进程数**: 建议不超过CPU核心数

### 3. 批处理策略
- **核心池**: 一次性处理 (通常50-200只股票)
- **全部股票**: 分批处理，每批50-100只
- **休息间隔**: 批次间休息5-10秒

## 故障排除

### 常见问题

1. **导入模块失败**
   ```
   解决方案: 确保在项目根目录运行脚本
   ```

2. **数据不足错误**
   ```
   原因: 股票历史数据少于250天
   解决方案: 跳过该股票或降低数据要求
   ```

3. **优化失败**
   ```
   原因: 算法不收敛或数据质量问题
   解决方案: 使用默认参数作为回退
   ```

4. **多进程错误**
   ```
   解决方案: 切换到单进程模式
   ```

### 日志文件

脚本会生成详细的日志文件：
- **文件名格式**: `profile_generation_YYYYMMDD_HHMMSS.log`
- **内容**: 详细的处理过程、错误信息、性能统计

## 集成使用

### 与通用筛选器集成

生成画像后，通用筛选器会自动使用优化参数：

```python
from backend.universal_screener import UniversalScreener

screener = UniversalScreener()
results = screener.run_screening(['abyss_strategy'])
# 自动使用每只股票的优化参数
```

### 与前端集成

画像数据存储在数据库中，前端可以通过API获取：

```javascript
// 获取股票画像信息
fetch(`/api/stock/${stockCode}/profile`)
  .then(response => response.json())
  .then(data => {
    console.log('优化参数:', data.optimized_params);
    console.log('验证分数:', data.validation_score);
  });
```

## 最佳实践

1. **首次使用**: 先为核心池生成画像，验证效果
2. **定期更新**: 每月更新一次画像数据
3. **性能监控**: 关注验证分数，剔除低分股票
4. **参数调优**: 根据实际效果调整优化算法参数
5. **数据备份**: 定期导出画像数据进行备份

## 下一步计划

1. **前端展示**: 在核心池和持仓详情中显示画像信息
2. **策略优化**: 基于画像数据优化交易策略
3. **实时更新**: 支持增量更新画像数据
4. **性能分析**: 添加画像效果的统计分析功能